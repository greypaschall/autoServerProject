# ServerlessMinecraft
An AWS hosted Minecraft Server. Listens for a TCP handshake, starts the server instance and tunnels connection to the server. Every component is stateless except the TCP listener.
